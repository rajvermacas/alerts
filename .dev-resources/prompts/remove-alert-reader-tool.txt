Right now there is a tool named #src/alerts/agents/insider_trading/agent.py#142 AlertReaderTool which i want to delete. 
Because currently the orchestrator accepts the payload with alert xml and then it passes to insider trading agent and then insider trading agent has a tool to read the xml using LLM and find important contextual data to pass onto to other tools.

The change i want is to pass the important contextual data from the big data layer itself which in our case happens to be the UI i think because the big data layer sits with another team so we just need to provide the api contract to them. 

Once only the contextual alert data is passed to the whole agentic system I don't think there is any requirement to have a separate alertreadertool.
------------------------

In the new AnalysisRequest sent from BigDataLayer i can see key_observations property which should not be there. big data layer should not make any analysis of the xml. It will just pass the raw xml in structured format with removing non relevant xml properties.

Answers to your questions:
1. LLM Narrative - Keep or Drop? - Drop
LLM Narrative is absolutely not needed because the alert data is anyway when sent to tool like trader profile or trader history or market news or market volatility there is already a LLM tool present inside the tool itself.

2. Who Maintains the Context Extraction Logic?
big data layer.
but for testing our backend we need to change the big data and ui layer which we have to accomodate the new changes of structured alert data instead of raw xml.

There will be no LLM called from inside the big data layer.

3. No Backward compatibility of any sort is accepted. NO shims accepted. 

4. Should we emit a synthetic context_received event for UI consistency? - Yes

